# S29.5 (`design/30-slices.md` § S29): `engines.node` in `package.json` is
# enforced, not advisory — `npm ci`/`npm install` refuses on a Node outside
# that range instead of silently running every gate on a different one.
engine-strict=true
